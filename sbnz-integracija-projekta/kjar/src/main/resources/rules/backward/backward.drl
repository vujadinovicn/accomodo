package backward;

import com.ftn.sbnz.backward.model.models.LocationBackward;

query isContainedIn( String x, String y )
  LocationBackward( x, y; )
  or
  ( LocationBackward( z, y; ) and isContainedIn( x, z; ) )
end

rule "go4"
when
    String( this == "go4" )
    isContainedIn(thing, "Serbia"; )
then
    System.out.println( "thing " + thing + " is in the Office" );
end